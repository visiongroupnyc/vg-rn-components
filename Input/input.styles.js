import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input_container: {
    marginBottom: 8,
    width: '100%',
  },
  input: {
    height: 44,
    borderWidth: 1,
    borderRadius: 4,
    padding: 8,
  },
});
